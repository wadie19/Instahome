<div class="container ">
    <div class="row d-flex justify-content-center" style="margin-top:20px">
    <div class="col-md-6" >
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-success text-center" style="font-family:cursive;">{{ button_label|default('Ajouter') }}</button>
            <a href="{{ path('articles_index') }}" style="font-family:cursive;" class="btn btn-secondary">
            <i class="glyphicon glyphicon-chevron-left"></i>
                Retour
            </a>
        {{ form_end(form) }}
        </div>
</div>
</div>
 